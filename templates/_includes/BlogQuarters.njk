<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KiwiMeowo's Blog Archive</title>
    <link rel="icon" type="image/x-icon" href="/favicon.ico?">
    <link href="../style.css" rel="stylesheet" type="text/css" media="all">
    <link href="../style-my-tooltips.css" rel="stylesheet" type="text/css" />
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.7/jquery.min.js"></script>
    <script src="../jquery.style-my-tooltips.js"></script>
    <style>
    body{
      background:var(--yellow);
    }
    :root{
      --yellow:#E2DBC7;
      --lteal:#1C3F5C77;
      --teal:#1C3F5C;
      --blue:#0E1A36;
      --dblue:#090E2B;
    }
    h1, h2{
    text-shadow:2px 2px #9DB7B5;
  }
.inner u{
  box-shadow: inset 0 -0.625em 0 0 #93aee673;
}
    </style>
  </head>
 <body>
    <div class="inner min blog">
    {% for post in collections.twentyfivei | reverse %}
        <a href="{{ post.url }}" target="_blank" class="collapsible{% if post.data.style %} special{% endif %}" title="{{ post.data.filter }}" {% if post.data.style %}style="{{ post.data.style }}"{% endif %}>{{ post.data.title }} <span class="blogdate">( {{ post.data.blogdate }} )</span></a>
        {% endfor %}
          </div>
</body>
</html>